import React from 'react';
// import css from './sublist.module.css';
// const data = ['1', '2', '3', '4', '5', '6'];
// import {useSelector} from 'react-redux';

const Task = () => {
  // const subList = useSelector(state => state.list);

  return (
    <>
      Task
      {/* {subList.list.length > 0
        ? subList.list.map(list => {
            return <div>{list}</div>;
          })
        : null} */}
    </>
  );
};

export default Task;
